# 2020-5-20
* Server Update
```
Change the ParzeXML and serverConect to correct the XML parse process.Then the Exceptions didn't print at the ParzeXML.But there still are some exceptions in socket.Maybe it's because of the wrong client.
我改写了ParzeXML和serverConect类，纠正了XML解析的过程。现在不再在ParzeXML上报错了。但是仍会报错，可能是客户端的锅。
```